# AGENTS.md

本文件定义在本仓库工作的 AI Agent 和开发者必须遵循的规则。

## 项目目标

复刻台用于把网页参考设计转化为可维护、可交互、可验证的前端实现。核心指标不是「看起来差不多」，而是有证据支持的高保真复刻，同时保留语义化结构、响应式能力和工程可维护性。

## 工作原则

1. 先理解参考页面，再写代码。记录信息架构、可见文案、视口、字体、颜色、间距、边框、圆角、阴影、资源和交互状态。
2. 先提取设计令牌和组件族，再实现页面。相同视觉元素必须共用组件或样式原语。
3. 真实 UI 文本、按钮和表单必须由 HTML/React 渲染；禁止把整页截图当作实现。
4. 优先使用现有组件和样式模式。仅在确实降低重复或复杂度时增加抽象。
5. 不要为了「更好看」改变参考设计。任何有意偏差都要在交付说明中列出。
6. 组件保持单一职责，`App.tsx` 只负责状态编排和页面组合。
7. 用户已有修改始终视为有效输入；不得回退或覆盖无关改动。

## 标准复刻流程

1. **采集证据**：保存目标视口截图，检查页面结构、字体和静态资源。
2. **建立规格**：锁定文案、页面顺序、布局、设计令牌、组件变体和交互状态。
3. **实现切片**：从首屏开始，按连续视口逐段实现，不要等整页写完才比较。
4. **运行验收**：启动本地应用，在真实浏览器中检查 DOM、控制台和交互。
5. **视觉比对**：在相同视口分别查看参考图和实现截图，逐项记录并修复差异。
6. **响应式检查**：至少验证一个桌面视口和一个移动视口。
7. **质量门禁**：运行 `npm run lint` 与 `npm run build`。

## 视觉验收清单

- 页面身份、导航、标题、按钮和可见文案一致
- 首屏比例、容器宽度、栅格、对齐和下一区域露出一致
- 字体家族、字号、字重、行高和换行一致
- 背景、文字、边框、阴影和强调色一致
- 图片裁切、位置、比例和混合方式一致
- 圆角、控件高度、图标语义和线条粗细一致
- hover、focus、selected、loading、success 等状态可用
- 无重叠、裁切、横向溢出、意外换行或布局跳动
- 控制台无相关 error 或 warning

完成视觉任务前必须保留一份简短差异账本：参考证据、实现证据、修复内容或有意偏差。至少覆盖布局、字体、颜色、间距、图标/资源和响应式中的五项。

## 工程约定

- 技术栈：React + TypeScript + Vite + 原生 CSS。
- 图标优先使用 `lucide-react`，不要使用文本字符模拟工具图标。
- 使用 `src/styles.css` 中的 CSS 变量维护全局设计令牌。
- 避免新增重量级依赖；新增前说明用途与替代方案。
- 不提交密钥、Cookie、访问令牌、`.env` 或目标站点私有资源。
- 默认使用 ASCII 编写代码与配置；中文只用于产品文案和文档。
- 所有按钮必须有明确行为；纯图标按钮需要 `aria-label` 或 `title`。
- 动画必须尊重 `prefers-reduced-motion`。

## 常用命令

```bash
npm install
npm run dev
npm run lint
npm run build
```

## 提交与交付

- 提交应聚焦单一目的，避免无关格式化和元数据变更。
- 交付说明包含：变更结果、测试命令、浏览器与视口、已验证交互、剩余限制。
- 参考驱动的实现必须附上参考概念路径和最终截图路径。
- 不得声称「一比一」或「像素级」完成，除非已在相同视口做截图比对且没有未说明的重大偏差。

## 安全与合规

- 只采集公开页面或用户有权访问和复刻的内容。
- 不绕过登录、验证码、付费墙、反自动化或访问控制。
- 尊重 robots、网站服务条款、版权、商标和个人数据保护要求。
- 外部网络受限时优先使用本地代理，但不得把代理地址、密钥或身份凭证写入仓库。
